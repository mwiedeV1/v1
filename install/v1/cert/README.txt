Insert SSL certificates and keys (PEM format) into this directory with following file format:

<host>.crt
<host>.key

If certificate is not found, then a self signed certificate will be created automatically by app/webserver.v1.